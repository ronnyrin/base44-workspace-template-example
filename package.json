{
  "name": "base44-workspace-template-example",
  "private": true,
  "version": "0.2.0",
  "description": "Example workspace template for Base44 App Templates. A plain Node HTTP server in TypeScript — no bundler, no UI framework — to demonstrate the pipeline works on any Node repo shape, not just Vite/React.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc --watch & node --watch dist/server.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.4"
  }
}
